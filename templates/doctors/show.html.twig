{% extends 'base.html.twig' %}

{% block title %}Doctors{% endblock %}

{% block body %}
    <h1>Doctors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doctor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ doctor.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ doctor.surname }}</td>
            </tr>
            <tr>
                <th>Specialty</th>
                <td>{{ doctor.specialty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('doctors_index') }}">back to list</a>

    <a href="{{ path('doctors_edit', {'id': doctor.id}) }}">edit</a>

    {{ include('doctors/_delete_form.html.twig') }}
{% endblock %}
